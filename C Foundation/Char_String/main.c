#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    /*定义字符串的几种方式
    //字符串和字符数组的区别：最后一位是空字符
    char name1[] = {'j','a','c','k','\0'};
    char name2[50] = {"jack"};
    printf("请输入新名称：");
    //gets(names2); //scanf("%s",names2);
    fgets(names2,10,stdin);//从标准流中读取10字节到数组names2中*/


    //常用字符串操作函数

    return 0;
}
